// lib/const/app_colors.dart

import 'package:flutter/material.dart';

abstract class AppColors {
  // ──────────────────────────────────────────────────────────────
  // PRIMARY
  // ──────────────────────────────────────────────────────────────

  // 🌑 Dark → White
  static const Color primaryDark = Color(0xFFFFFFFF);
  static const Color onPrimaryDark = Color(0xFF141414);
  static const Color primaryContainerDark = Color(0xFF2A2A2A);
  static const Color onPrimaryContainerDark = Color(0xFFFFFFFF);

  // ☀️ Light → Olive Green
  static const Color primaryLight = Color(0xFF586400);
  static const Color onPrimaryLight = Color(0xFFFFFFFF);
  static const Color primaryContainerLight = Color(0xFFE6EDB5);
  static const Color onPrimaryContainerLight = Color(0xFF1F2400);

  // ──────────────────────────────────────────────────────────────
  // SECONDARY
  // ──────────────────────────────────────────────────────────────

  static const Color secondaryDark = Color(0xFFB00FF9);
  static const Color onSecondaryDark = Color(0xFFFFFFFF);
  static const Color secondaryContainerDark = Color(0xFF3A0050);
  static const Color onSecondaryContainerDark = Color(0xFFB00FF9);

  static const Color secondaryLight = Color(0xFF121212);
  static const Color onSecondaryLight = Color(0xFFFFFFFF);
  static const Color secondaryContainerLight = Color(0xFFE0E0E0);
  static const Color onSecondaryContainerLight = Color(0xFF121212);

  // ──────────────────────────────────────────────────────────────
  // TERTIARY
  // ──────────────────────────────────────────────────────────────

  static const Color tertiaryDark = Color(0xFF44C5F7);
  static const Color onTertiaryDark = Color(0xFF141414);
  static const Color tertiaryContainerDark = Color(0xFF003A4D);
  static const Color onTertiaryContainerDark = Color(0xFF44C5F7);

  static const Color tertiaryLight = Color(0xFFCBFF47);
  static const Color yellow = Color(0xFFD8EE36);
  static const Color onTertiaryLight = Color(0xFF141414);
  static const Color tertiaryContainerLight = Color(0xFFF0FF80);
  static const Color onTertiaryContainerLight = Color(0xFF1A2900);

  // ──────────────────────────────────────────────────────────────
  // NEUTRAL
  // ──────────────────────────────────────────────────────────────

  static const Color neutralDark = Color(0xFF777888);
  static const Color neutralLight = Color(0xFFF4F4F4);

  // ──────────────────────────────────────────────────────────────
  // SURFACES
  // ──────────────────────────────────────────────────────────────

  // 🌑 Dark
  static const Color surfaceDark = Color(0xFF141414);
  static const Color surfaceHighDark = Color(0xFF1E1E1E);
  static const Color surfaceHighestDark = Color(0xFF2A2A2A);

  static const Color onSurfaceDark = Color(0xFFFFFFFF);
  static const Color onSurfaceVariantDark = Color(0xFF9E9E9E);

  // ☀️ Light
  static const Color surfaceLight = Color(0xFFFFFFFF);
  static const Color surfaceHighLight = Color(0xFFF7F7F7);
  static const Color surfaceHighestLight = Color(0xFFEEEEEE);

  static const Color onSurfaceLight = Color(0xFF141414);
  static const Color onSurfaceVariantLight = Color(0xFF6B6B6B);

  // ──────────────────────────────────────────────────────────────
  // OUTLINE
  // ──────────────────────────────────────────────────────────────

  static const Color outlineDark = Color(0xFF3A3A3A);
  static const Color outlineVariantDark = Color(0xFF2A2A2A);

  static const Color outlineLight = Color(0xFFDDDDDD);
  static const Color outlineVariantLight = Color(0xFFEEEEEE);

  // ──────────────────────────────────────────────────────────────
  // SEMANTIC
  // ──────────────────────────────────────────────────────────────

  static const Color error = Color(0xFFE53935);
  static const Color onError = Color(0xFFFFFFFF);

  static const Color success = Color(0xFF43A047);
  static const Color warning = Color(0xFFFFA726);
  static const Color info = Color(0xFF29B6F6);

  // ──────────────────────────────────────────────────────────────
  // HINT TEXT
  // ──────────────────────────────────────────────────────────────

  // 🌑 Dark → #8888A0
  static const Color hintDark = Color(0xFF8888A0);

  // ☀️ Light → #5F5E5E
  static const Color hintLight = Color(0xFF5F5E5E);

  static const Color artichoke = Color(0xff91937A);
  static const Color darkOvel = Color(0xff464834);
  static const Color cardColor = Color(0xff13131A);
}
